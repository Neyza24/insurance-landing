---
import { Image } from "astro:assets";
import superintendenciaImg from "../assets/superlogo.png";

---

<!-- Hero Section -->
<section class="pt-8 pb-8 relative overflow-hidden bg-linear-to-b from-primary-100 to-white dark:from-primary-950 dark:to-secondary-950">
  <div class="absolute inset-0 z-0 opacity-30">
    <div class="absolute inset-0 bg-grid-pattern"></div>
  </div>
  
  <div class="container-custom relative z-10">
    <div class="mx-auto px-4 flex flex-col items-center text-center">
      <h2 class="text-2xl md:text-3xl mb-4 md:mb-6 text-gray-900 dark:text-white animate-slide-down">
       Contamos con <span class="text-accent-500">Certificado Circular 050</span>
      </h2>
      <p class="text-lg md:text-xl text-gray-700 dark:text-gray-300 mb-6 md:mb-8 animate-slide-up" style="animation-delay: 200ms">
        La <b>Circular 050 de la Superfinanciera</b>, garantiza que recibas asesoría en seguros de un profesional certificado, capacitado y comprometido con proteger tus derechos.
      </p>
        <div class="mb-6 animate-fade-in" style="animation-delay: 400ms">
            <Image src={superintendenciaImg} alt="Certificado Circular 050 de la Superintendencia Financiera" class="w-60 mx-auto"/>
        </div>
  </div>

  <!-- Animated Shapes -->
  <div class="absolute -bottom-16 left-0 right-0 flex justify-center">
    <div class="w-48 md:w-64 h-48 md:h-64 bg-accent-400 rounded-full filter blur-3xl opacity-20 animate-pulse"></div>
  </div>
</section> 